<%= form_for(@subtitle_track_set) do |f| %>
  <% if @subtitle_track_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subtitle_track_set.errors.count, "error") %> prohibited this subtitle_track_set from being saved:</h2>

      <ul>
      <% @subtitle_track_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
